UCORE-MP64 is a branch version of the 64-bit ucore (by Wang,
Naizheng). The branch is mainly for running on the different
multi-processor architecture.

Different with original ucore, UCORE-MP64 is a snapshot style (as
linear hierachy style of ucore), which the snapshot is deviant from
the last lab from the ucore repository.
